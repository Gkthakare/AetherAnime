/**
 * RegionIdentity — types only.
 *
 * Presentation of the focused Region. Observes RegionScene only.
 */

export type RegionIdentityProps = {
  /** Layout composition only. */
  className?: string;
};
