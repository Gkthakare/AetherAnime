/**
 * RegionActivities — types only.
 *
 * Presentation of canonical Region activity tokens. Observes RegionScene only.
 */

export type RegionActivitiesProps = {
  /** Layout composition only. */
  className?: string;
};
