/**
 * RegionScene — Region runtime director for World Experience.
 *
 *   import { RegionScene } from '@/widgets/region-scene';
 */

export { RegionScene } from './region-scene';
export type {
  RegionSceneContextValue,
  RegionSceneProps,
  RegionSceneStatus,
} from './region-scene.types';
