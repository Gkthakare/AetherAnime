/**
 * RegionClimate — types only.
 *
 * Atmosphere presentation for the focused Region. Observes RegionScene only.
 */

export type RegionClimateProps = {
  /** Layout composition only. */
  className?: string;
};
